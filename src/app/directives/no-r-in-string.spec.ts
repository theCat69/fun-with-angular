import { NoRInString } from './no-r-in-string';

describe('NoRInString', () => {
  it('should create an instance', () => {
    const directive = new NoRInString();
    expect(directive).toBeTruthy();
  });
});
